#!/bin/bash
set -e

post_install() {
	cd /etc/skel/.local/share/
	dconf load /org/xfce/mousepad/ < mousepad.settings.dconf
    echo -e "\n** The files have been installed in ~/.local/share/ **\n"
}

post_upgrade() {
    post_install
}
